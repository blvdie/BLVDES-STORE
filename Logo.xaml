<Page x:Class="KURSOVAYA_RABOTA.Logo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KURSOVAYA_RABOTA"
      mc:Ignorable="d" 
       Width="1200" Height="800"
      Title="Logo">

    <Page.Resources>
        <Storyboard x:Key="LogoAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:1.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.5" To="1" Duration="0:0:1.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.5" To="1" Duration="0:0:1.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                             From="0" To="360" Duration="0:0:5" RepeatBehavior="Forever"/>
        </Storyboard>

        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.8"/>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Image Source="/images/logo.png" Width="320" Height="246" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,157,0,0"/>

        <!-- Основное содержимое D:\WEB\КУРСАЧ БЕЙБА\BlvdesStore\images\store_logo_new.png
        "D:\WEB\КУРСАЧ БЕЙБА\BlvdesStore\images\store_logo.png"
        "D:\WEB\КУРСАЧ БЕЙБА\BlvdesStore\images\back_logo.png"-->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center"  Margin="0,424,0,0">
            <TextBlock Text="BLVDES STORE" 
                       FontSize="60" 
                       Foreground="#be8dee" 
                       FontWeight="Bold" 
                       TextAlignment="Center"/>
            <TextBlock Text="Скины из Dota 2 и CS:GO" 
                       FontSize="20" 
                       Foreground="#E0E0E0" 
                       TextAlignment="Center"/>

        </StackPanel>
    </Grid>
</Page>
